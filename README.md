# ⚽ Прогнозирование результатов футбольных матчей (Английская Премьер-Лига)
**Дипломный проект по курсу "AI/ML разработчик"**  
*Полный цикл: от сбора данных и EDA до сравнения классических моделей и нейросетей*

## 📌 О проекте

Прогнозирование результатов футбольных матчей с использованием методов машинного обучения на материале Английской Премьер-лиги.  
**Задача:** многовариантная регрессия для предсказания:
- Количества голов домашней (`goals_home`) и гостевой (`goals_away`) команд
- Исхода матча (без счета)
- Тоталов (>2.5, >3.5, >4.5)
- "Обе забьют"

**Метрики качества:** MAE (Mean Absolute Error), R² (коэффициент детерминации)

## 🎯 Ключевые результаты

### 📊 Модель-победитель: **LinearRegression (на 6 сезонах)

| Метрика | Значение |
|---------|----------|
| **R² (точный счет)** | 0.1297 |
| **MAE** | 0.9288 |
| **Точность исходов** | 54.27% |
| **Тотал >2.5** | 57.55% |
| **Тотал >3.5** | 63.24% |
| **Тотал >4.5** | 82.49% |
| **Обе забьют** | 56.67% |

> **Интерпретация:** Модель справляется с прогнозированием **в 6.5 раз лучше**, чем случайное угадывание (2%). Результаты сопоставимы с букмекерскими котировками.

# Источники данных
- [Football-Data.co.uk](http://Football-Data.co.uk) — исторические данные по матчам АПЛ (сезоны 1993/94 – 2024/25).
- [Clubelo.com](http://clubelo.com) — рейтинги Эло команд на разные даты.

Данные не являются конфиденциальными, источники открыты для некоммерческого использования.

Период выборки:**  

32 сезона (1993/94 – 2024/25) — полная выборка.

В финальной версии используется ограниченная выборка: **6 сезонов (2019/20 – 2024/25).** Модель, обученная именно на этом количестве сезонов, показала наилучший результат в исследовании.

В качестве гипотез были проверены выборки от 3 до 32 сезонов.
# Параметризация данных
## Этапы предобработки
1) Очистка от пропусков и выбросов.
2) Кодирование категориальных признаков (названия команд).
3) Исходные признаки:
- Дата матча.
- Названия команд.
- Итоговый счет.
4) Feature Engineering:
- Скользящие средние забитых/пропущенных голов за последние 5 матчей (дома и в гостях).
- Форма команды (очки в последних турах).
- Личные встречи команд (H2H).
- Рейтинг Эло команд перед началом матча.
- Разница набранных очков в последних матчах.


# Архитектура модели
В финальной версии работы модели используется алгоритм классического машинного обучения, **LinearRegression.** Также в качестве гипотез были проверены модели:

- Регуляризованные модели: `Ridge`, `Lasso`,
- Ансамбли: `RandomForestRegressor`, `GradientBoostingRegressor`, `DecisionTreeRegressor`.
- Нейросети Sequential с 5 разными архитектурами:

  |**Название**|**Слои**|
  | :- | :- |
  |Simple|<p>- Dense(128, activation='relu', input\_shape=(input\_dim,)),</p><p>- Dropout(0.3),</p><p>- Dense(64, activation='relu'),</p><p>- Dropout(0.2),</p><p>- Dense(32, activation='relu'),</p><p>- Dense(1, activation='linear')</p>|
  |Light|<p>- Dense(64, activation='relu', input\_shape=(input\_dim,)),</p><p>- Dropout(0.3),</p><p>- Dense(32, activation='relu'),</p><p>- Dropout(0.2),</p><p>- Dense(16, activation='relu'),</p><p>- Dense(1, activation='linear')</p>|
  |Micro|<p>- Dense(32, activation='relu', input\_shape=(input\_dim,)),</p><p>- Dropout(0.2),</p><p>- Dense(16, activation='relu'),</p><p>- Dropout(0.1),</p><p>- Dense(1, activation='linear')</p><p></p>|
  |Shallow|<p>- Dense(48, activation='relu', input\_shape=(input\_dim,)),</p><p>- Dropout(0.25),</p><p>- Dense(1, activation='linear')</p>|
  |Mini|<p>- Dense(24, activation='relu', input\_shape=(input\_dim,)),</p><p>- Dense(12, activation='relu'),</p><p>- Dense(1, activation='linear')</p>|


## <a name="_vf2k0a2icmon"></a>Процесс обучения
- Разделение данных на обучающую/тестовую выборки с учетом временных рядов.
- Масштабирование признаков (StandardScaler).
- Обучение отдельно для каждой целевой переменной.

# <a name="_j8f6aqwb0hxr"></a>Графическое подтверждение
Визуализации:

- Симуляция турнирной таблицы последнего сезона АПЛ на основе предсказаний модели.
- Сравнение смоделированных и реальных позиций команд в турнирной таблице.

Примеры графиков:  



# <a name="_4hkxpvwwtvli"></a>Выводы

1. Наилучшие результаты показала модель LinearRegression на 6 сезонах, она будет использована в качестве финальной. 
  Результаты модели:
  - R² (точный счет): 0.1297
  - MAE: 0.9288
  - Точность исходов: 0.5427
  - Тотал >2.5: 0.5755
  - Тотал >3.5: 0.6324
  - Тотал >4.5: 0.8249
  - Обе забьют: 0.5667
2. Результаты в целом соответствуют букмекерским котировкам на матч: на точный счет коэффициент 7.7 (1 / 0.1297) - нормальная практика. Модель выполняет свою функцию базового предсказания лучше простого угадывания. Это показывает и симуляция таблицы сезона 24/25, в которой большинство команд оказались на тех же позициях, что и в реальной жизни. Исключением стали МЮ и "Тоттенхэм", которые были скорее аномалиями, т.к. команды в этом сезоне фокусировались на еврокубках, где показывали сильные результаты.
3. В ходе исследования были отвергнуты гипотезы:
- влияние домашнего преимущества (оно минимально).
- влияние статуса дерби.
- влияние количества дней до конца сезона.
- влияние статистических показателей в последних матчах (удары, угловые, карточки).

  Также было принято решение отказаться от конфигурации "с послематчевой статистикой", так как она не отражает реального процесса прогнозирования и решает другие задачи.

4. Наибольшую корреляцию с итоговым результатом показывают признаки:
- рейтинг Эло перед матчем (его добавление внесло решающий вклад в улучшение качества прогноза).
- форма команд (забитые и пропущенные голы в последних матчах).
- история встреч команд друг с другом. 
5. Глобального влияния количества сезонов на результат не выявлено, однако лучшие показатели получились у моделей с небольшим количеством сезонов (6, 3, 5, 7). Это может говорит о том, что футбол чувствителен к смене многих факторов: правил, судейства, стиля игры команд, экономической ситуации, из-за чего более старые сезоны лишь сбивают модель.
6. Классические модели справляются с задачей в целом лучше, чем нейросети. Это говорит о том, что задача прогнозирования матчей не включает в себя сложных закономерностей.
7. Модель можно использовать как рабочую для прогнозирования тоталов и исходов. Существенное повышение R² по точному счету не представляется возможным из-за огромного количества возможных  счетов (50+) и факторов неопределенности: отскоки, судейские ошибки, погодные условия, и др. Вероятность случайного угадывания - от 2%. Следовательно, наша модель справляется с прогнозированием примерно в 6,5 раз лучше, что сопоставимо с прогнозами букмекерских контор и экспертов.
8. Тем не менее можно добиться точечного улучшения результатов путем добавления более сложных показателей:
- xG (ожидаемые голы).
- тренерские показатели (очки за игру, общий рейтинг тренера, количество дней в команде, приход нового тренера).
- трансферные стоимости составов команд (требует сложной настройки API).


# 🚀 Как запустить проект
1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/Reaktiv96/Football-Match-Predictor.git
2. Установите зависимости:
bash
pip install -r requirements.txt
3. Откройте и запустите блокноты в папке /notebooks последовательно.


